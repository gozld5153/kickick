import React from "react";
import Notice from "./";

export default {
  title: "PAGES/Notice",
  component: Notice,
};

const Template = (args) => <Notice {...args} />;

export const Control = Template.bind({});
