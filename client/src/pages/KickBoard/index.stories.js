import React from "react";
import KickBoard from "./";

export default {
  title: "PAGES/KickBoard",
  component: KickBoard,
};

const Template = (args) => <KickBoard {...args} />;

export const Control = Template.bind({});
