import React from "react";

import Modal from "./";

export default {
  title: "Atoms/Modal",
  component: Modal,
};

const Template = (args) => <Modal {...args} />;

export const Control = Template.bind({});
