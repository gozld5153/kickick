import React from "react";
import AlienPortion from ".";

export default {
  title: "atoms/Graph/AlienPortion",
  component: AlienPortion,
};

const Template = (args) => <AlienPortion {...args} />;

export const Control = Template.bind({});
