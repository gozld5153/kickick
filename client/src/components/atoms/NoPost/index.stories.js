import React from "react";

import NoPost from "./";

export default {
  title: "Atoms/NoPost",
  component: NoPost,
};

const Template = (args) => <NoPost {...args} />;

export const Control = Template.bind({});
