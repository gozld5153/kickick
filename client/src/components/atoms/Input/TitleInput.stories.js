import React from "react";

import TitleInput from "./TitleInput";

export default {
  title: "Atoms/Input/TitleInput",
  component: TitleInput,
};

const Template = (args) => <TitleInput {...args} />;

export const Control = Template.bind({});
