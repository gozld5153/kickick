import React from "react";

import Spinner from "./";

export default {
  title: "Atoms/LoadingSpinner/Spinner",
  component: Spinner,
};

const Template = (args) => <Spinner {...args} />;

export const Control = Template.bind({});
