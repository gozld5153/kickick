import React from "react";

import S_BoardTop from "./S_BoardTop";

export default {
  title: "Skeleton/S_BoardTop",
  component: S_BoardTop,
};

const Template = (args) => <S_BoardTop {...args} />;

export const Control = Template.bind({});
