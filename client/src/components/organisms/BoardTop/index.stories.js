import React from "react";

import BoardTop from "./";

export default {
  title: "Organisms/BoardTop",
  component: BoardTop,
};

const Template = (args) => <BoardTop {...args} />;

export const Control = Template.bind({});
