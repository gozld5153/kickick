import React from "react";

import S_BoardBottom from "./S_BoardBottom";

export default {
  title: "Skeleton/S_BoardBottom",
  component: S_BoardBottom,
};

const Template = (args) => <S_BoardBottom {...args} />;

export const Control = Template.bind({});
