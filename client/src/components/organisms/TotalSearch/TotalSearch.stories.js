import React from "react";
import TotalSearch from "./TotalSearch";

export default {
  title: "Organisms/TotalSearch",
  component: TotalSearch,
};

const Template = (args) => <TotalSearch {...args} />;

export const Control = Template.bind({});
