import React from "react";

import S_TotalSearch from "./S_TotalSearch";

export default {
  title: "Skeleton/S_TotalSearch",
  component: S_TotalSearch,
};

const Template = (args) => <S_TotalSearch {...args} />;

export const Control = Template.bind({});
